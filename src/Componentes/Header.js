import React from 'react';

const Header = () => {
  return (
    <header className="dashboard">
        <h1 className="dashboard-title"> Panel de Control</h1>   {/* Título de la página */}
    </header>
  );
};

export default Header;